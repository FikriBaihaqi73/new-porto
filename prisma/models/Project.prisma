model Project {
  id          String    @id @default(uuid())
  name        String
  description String?
  link        String?
  image       String?
  userId      String
  user        User      @relation(fields: [userId], references: [id])
}
